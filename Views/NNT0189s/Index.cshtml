@model IEnumerable<nguyennhuthin189.Models.NNT0189>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NNTName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NNTGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NNTName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NNTGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NNTId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NNTId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NNTId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
